<div class="dashboard-shell" id="dashboardShell">
  <div class="sidebar-backdrop" id="sidebarBackdrop"></div>

  <aside class="sidebar" id="sidebarNav">
    <a href="/dashboard" class="sidebar-brand">
      <span class="brand-dot"></span>
      <span>Smart Task Manager</span>
    </a>

    <nav class="sidebar-menu">
      <a href="/dashboard" class="menu-item">
        <span class="menu-icon">📊</span>
        <span>Overview</span>
      </a>
      <a href="/dashboard/profile" class="menu-item">
        <span class="menu-icon">👤</span>
        <span>View Profile</span>
      </a>
      <a href="/dashboard/profile/edit" class="menu-item">
        <span class="menu-icon">✏️</span>
        <span>Edit Profile</span>
      </a>
      <a href="/dashboard?status=Pending" class="menu-item">
        <span class="menu-icon">🕒</span>
        <span>Pending Tasks</span>
      </a>
      <a href="/dashboard?status=Completed" class="menu-item">
        <span class="menu-icon">✅</span>
        <span>Completed Tasks</span>
      </a>
    </nav>

    <div class="sidebar-footer">
      <form action="/logout" method="POST">
        <button class="btn btn-ghost sidebar-logout" type="submit">Logout</button>
      </form>
    </div>
  </aside>

  <div class="dashboard-content-area">
    <header class="dashboard-topbar">
      <div class="topbar-left">
        <button class="menu-toggle" id="menuToggle" aria-label="Open sidebar">☰</button>
        <div class="topbar-title-wrap">
          <p class="topbar-kicker">Workspace</p>
          <div class="topbar-title">Smart Task Dashboard</div>
        </div>
      </div>

      <div class="topbar-actions">
        <button class="theme-toggle" id="themeToggle" type="button" aria-label="Toggle dark mode">🌙</button>

        <div class="profile-dropdown" id="profileDropdown">
          <button class="profile-btn" id="profileBtn" type="button" aria-expanded="false">
            <span class="avatar"><%= currentUser.username.slice(0, 1).toUpperCase() %></span>
            <span><%= currentUser.username %></span>
            <span>▾</span>
          </button>
          <div class="profile-menu" id="profileMenu">
            <a href="/dashboard/profile">View Profile</a>
            <a href="/dashboard/profile/edit">Edit Profile</a>
            <form action="/logout" method="POST">
              <button class="profile-logout" type="submit">Logout</button>
            </form>
          </div>
        </div>
      </div>
    </header>
