<div class="dashboard-shell" id="dashboardShell">
  <aside class="sidebar" id="sidebarNav">
    <a href="/tasks" class="sidebar-brand">⚡ Smart Task</a>

    <nav class="sidebar-menu">
      <a href="/tasks" class="menu-item">
        <span class="menu-icon">📊</span>
        <span>Dashboard</span>
      </a>
      <a href="/tasks/new" class="menu-item">
        <span class="menu-icon">➕</span>
        <span>New Task</span>
      </a>
      <a href="/tasks?status=Pending" class="menu-item">
        <span class="menu-icon">🕒</span>
        <span>Pending</span>
      </a>
      <a href="/tasks?status=Completed" class="menu-item">
        <span class="menu-icon">✅</span>
        <span>Completed</span>
      </a>
    </nav>

    <div class="sidebar-footer">
      <form action="/logout" method="POST">
        <button class="btn btn-ghost sidebar-logout" type="submit">Logout</button>
      </form>
    </div>
  </aside>

  <div class="dashboard-content-area">
    <header class="dashboard-topbar">
      <button class="menu-toggle" id="menuToggle" aria-label="Open sidebar">☰</button>
      <div class="topbar-title">Task Workspace</div>

      <div class="profile-dropdown" id="profileDropdown">
        <button class="profile-btn" id="profileBtn" type="button" aria-expanded="false">
          <span class="avatar"><%= currentUser.username.slice(0, 1).toUpperCase() %></span>
          <span><%= currentUser.username %></span>
          <span>▾</span>
        </button>
        <div class="profile-menu" id="profileMenu">
          <a href="/tasks">My Dashboard</a>
          <a href="/tasks/new">Create Task</a>
          <form action="/logout" method="POST">
            <button class="profile-logout" type="submit">Logout</button>
          </form>
        </div>
      </div>
    </header>
